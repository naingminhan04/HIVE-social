import { Gift } from "lucide-react";
import WorkInProgressPlaceholder from "@/app/_components/common/WorkInProgressPlaceholder";

const RedeemPage = () => {
  return (
    <WorkInProgressPlaceholder
      title="Redeem Coming Soon"
      description="Rewards and point redemption will live here once the available redeem items and usage flow are wired to the backend."
      icon={Gift}
    />
  );
};

export default RedeemPage;
