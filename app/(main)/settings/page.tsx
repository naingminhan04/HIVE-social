import { Settings } from "lucide-react";
import WorkInProgressPlaceholder from "@/app/_components/WorkInProgressPlaceholder";

const SettingsPage = () => {
  return (
    <WorkInProgressPlaceholder
      title="Settings Coming Soon"
      description="Account, devices, terms, privacy, and app update tools will live here once those screens are ready."
      icon={Settings}
    />
  );
};

export default SettingsPage;
